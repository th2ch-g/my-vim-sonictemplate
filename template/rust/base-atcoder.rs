use proconio::{*};
use proconio::marker::{*};
use std::collections::{*};
use petgraph::algo::{*};
use petgraph::graph::{*};
use petgraph::unionfind::{*};
use petgraph::data::{*};
#[fastout]
fn main() {
    input! {
        n: usize,
    }

    {{_cursor_}}

}
