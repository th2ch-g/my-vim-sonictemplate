use proconio::{*};
use proconio::marker::{*};
use std::collections::{*};
use petgraph::algo::{*};
use petgraph::graph::{*};
use petgraph::unionfind::{*};
use petgraph::data::{*};
use itertools::{*};
#[fastout]
fn main() {
    input! {
        n: usize,
    }

    {{_cursor_}}

}
